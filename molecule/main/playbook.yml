---

- name: Converge
  hosts: all
  connection: local

  vars_files:
    - ../resources/vars/main.yaml

  roles:
    - role: geerlingguy.python-pip
    - role: ansible-role-prometheus-pgbouncer-exporter
